# Окружение приложения: local, development, production
env: "local"

# Путь к локальному хранилищу данных
storage_path: "./storage/storage.db"

# Настройки HTTP-сервера
http_server:
  address: "localhost:8082"
  timeout: 4s
  idle_timeout: 60s